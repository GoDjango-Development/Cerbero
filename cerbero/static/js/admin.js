$(document).ready(function () {
    // Crear la tabla con DataTables
    var table = $('#data').DataTable({

            paging: false,
        scrollCollapse: true,
        scrollY: '200px',
        scrollX: true,
    });

});
